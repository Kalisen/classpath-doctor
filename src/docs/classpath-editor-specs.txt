edit classpath widget

features
	support jars and directories
	support env variable resolution
		display value in tooltip
	autodetect + configurable windows / unix path separator
	add / remove entries
	detect duplicates
	detect potential conflicts
		same library name in different location
		same library name but different version
		
optional features
	picture customization
	connect to running process


	